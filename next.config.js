// Import the NextConfig type for better autocompletion
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['https://uploading-stuff-cloud.s3.eu-west-1.amazonaws.com'],
    },
    // Include any other configuration options here
  };
  
  module.exports = nextConfig;
  